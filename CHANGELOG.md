# CHANGELOG

## [Unreleased]

### Added (追加機能)
- **タイムストレッチ（倍速化）の高品質化**:
  - `pedalboard` (Rubberband) を内部に組み込み、VOICEVOXエディタからの「1.2倍速以上」の指定に対して、AIの幻覚暴走を防ぎつつ高音質なフェーズボコーダーで倍速化するハイブリッド機構を実装しました。
  - さらにタイムストレッチの設定を「ボーカル・音声専用（smooth/soft/time_domain_smoothing）」にチューニングし、倍速時のジャギジャギとしたノイズを極限まで低減しました。
- **ユーザー辞書の即時反映（キャッシュクリア）機能**:
  - 辞書に単語を追加・編集・削除した瞬間に、音声合成エンジンのキャッシュを強制クリアする処理を追加。これにより辞書更新後すぐに新しい読みが音声に反映されるようになりました。

### Fixed (バグ修正)
- **辞書の全角・半角・大文字・小文字の不一致問題**:
  - ユーザー辞書の登録語と本文テキストの両方に「NFKC正規化」および大文字小文字無視を適用。エディタが強制的に「１，０００」や「ｉｒｏｄｏｒｉ」と全角保存しても、本文の「1,000」や「Irodori」に完璧にマッチするようになりました。
- **エディタ上のアクセント句（カタカナ表示）のバグ修正**:
  - OpenJTalkが解析を行う**前**の段階で辞書の置換処理（reading_overrides）をかけるように変更。これにより、エディタ画面下のカタカナ表示にも辞書の読みが正しく反映されるようになりました。
  - さらに、`/accent_phrases` エンドポイントが `is_kana=True` の時に、複数のモーラを1つのアクセント句として正しく結合するように修正。辞書のアクセント調整グラフが途切れる問題（2文字しか入らない問題）が直りました。
- **シード値音声（お手本なし生成）の安定化**:
  - シード値で音声を生成する際、AIのテキストへの忠実度（`cfg_scale_text`）を強制的に `5.0` へ底上げするパッチを適用し、辞書指定の読み飛ばしや幻覚を大幅に抑え込みました。
- **ビルド・セットアップ用スクリプトの修正**:
  - `setup-voicevox-editor.bat` 内で、管理画面（`admin`）用の依存パッケージも正しく `pnpm install` されるように修正（`typescript` 等の不足によるコンパイルエラーを解決）。
  - `downloadTypos.ts` 内の7-zip解凍処理で、パスに空白が含まれているとエラーになる問題を解消（パスをクォーテーションで囲むように修正）。